# Airflow and core dependencies
apache-airflow>=2.7.0,<3.0.0
apache-airflow-providers-postgres>=5.0.0

# HTTP requests
requests>=2.31.0

# Date utilities
python-dateutil>=2.8.2

# PostgreSQL adapter (usually included with Airflow provider, but explicit for clarity)
psycopg2-binary>=2.9.0
